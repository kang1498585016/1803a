<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bw.mapper.ParMapper">
	<select id="list"  resultType="com.bw.mapper.ParMapper">
        SELECT par.*,dis.names from par LEFT JOIN dis on par.did = dis.did
    </select>
	
</mapper>